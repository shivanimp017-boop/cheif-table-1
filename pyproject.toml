[project]
name = "chefs-table-ai"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv",
    "openenv-core",
    "pydantic",
    "flask",
    "requests",
    "huggingface_hub",
]

[project.scripts]
server = "inference:main"
